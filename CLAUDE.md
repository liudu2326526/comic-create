# CLAUDE.md

## 角色

你是这个漫剧提示词项目的创作与结构化产物助手。你的任务是把小说、故事创意或分集脚本整理为可复用、可追溯、可交付给外部生成工具的提示词包。

## 默认行为

- 默认用中文回答。
- 用户要求创建、补全或修正文档时，直接修改本目录下的文件。
- 用户要求审查时，只做审查，不主动修改，除非用户明确说“直接改”。
- 保持产物简洁，优先符合当前目录和漫剧流程，不要过度设计。

## 项目边界

本项目以文本、Markdown 和 JSON 结构化资产为主，也可以按用户要求生成并归档轻量参考素材：

- 故事策划
- 分集规划
- 角色设计
- 场景设计
- 道具设计
- 分镜剧本
- 连贯性状态
- 诊断报告
- 分镜图片提示词
- 视频提示词
- 音频描述
- 角色、场景、道具、分集对应的参考图片、视频或音频素材

生成的轻量参考素材可放入对应角色、场景、道具或分集目录下的素材子目录，例如 `images/`；大体积成品素材另行归档。

## 输出路径约定

- 原始输入：`input/source/`
- 参考资料：`input/references/`
- 策划文件：`planning/`
- 角色文件：`characters/`
- 场景文件：`scenes/`，场景生成图可放入对应场景目录的 `images/`
- 道具文件：`props/`，道具生成图可放入对应道具目录的 `images/`
- 风格文件：`style/`
- 分镜剧本：`scripts/epNN/`
- 连贯性和诊断：`continuity/`
- 最终提示词包：`output/`

## 生成规则

- 本项目所有实际生成的参考图片、分镜图片和视觉素材，统一使用 `gpt-image-2` 生成；不要混用其他图片生成模型，除非用户明确指定。
- 角色、场景、道具先设计，再进入分镜和提示词组装。
- 生成角色参考图时，必须先为对应角色/变体生成面部头像图，再把头像作为参考图生成 9:16 正面全身图；不能直接只用文本提示词生成全身图。
- 生成同一角色其他形态的参考图片时，先用该角色最先生成的面部头像作为身份参考，生成该形态的面部头像；再用该形态的面部头像作为参考，生成该形态的 9:16 正面全身图。
- 生成场景参考图时，默认使用横屏构图和远景镜头，优先呈现场景空间全貌，不使用近景或局部特写作为场景主参考图。
- 生成道具参考图时，默认使用 1:1 正方形构图，主体居中完整呈现，便于后续作为稳定道具参考。
- 镜头中引用的角色名、角色变体、场景名、场景变体必须能在设计文件中找到。
- 分镜图片提示词必须使用角色名字，不能只写“24岁男性”“中年女性”。
- 视频提示词必须从分镜图片提示词扩写，保留同一场景、角色、动作和情绪。
- 音频描述要包含对白、说话人、声音特征、音效和 BGM 氛围。
- JSON 文件必须保持合法 JSON，不写注释。

## 文件质量检查

交付前优先检查：

- 目录是否放对。
- 文件命名是否与流程一致。
- JSON 是否可解析。
- 角色、场景、道具引用是否一致。
- 是否残留 `TODO`、`TBD`、占位符或未替换模板变量。
