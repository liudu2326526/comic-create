# AGENTS.md

## 项目定位

本目录用于沉淀漫剧创建流程的全部中间产物、参考素材和最终提示词包。当前项目负责组织创作资料、剧本结构、角色/场景/道具设定、分镜剧本、诊断记录、提示词输出，以及按需归档生成的轻量参考图片、视频或音频素材。

## 工作语言

- 默认使用中文沟通和写作文档。
- JSON 字段名保持英文，字段值优先使用中文自然语言。
- 提示词必须使用中文自然语言描述，避免英文标签堆叠。

## 标准目录

- `input/source/`：原始小说、故事文本、用户创意、原文拆分稿。
- `input/references/`：视觉参考、风格参考、平台要求、补充说明。
- `planning/`：故事大纲、分集规划。
- `characters/`：角色设计卡。按 `characters/{项目名}/{角色名}/{角色名}.json` 存放设定文件；同一角色后续生成的图片、视频分别放入该角色目录下的 `images/`、`videos/`。
- `scenes/`：场景设计卡。按 `scenes/{项目名}/{场景名}/{场景名}.json` 存放设定文件；同一场景后续生成的图片、视频分别放入该场景目录下的 `images/`、`videos/`。
- `props/`：关键道具设计卡。按 `props/{项目名}/{道具名}/{道具名}.json` 存放设定文件；同一道具后续生成的图片、视频分别放入该道具目录下的 `images/`、`videos/`。
- `style/`：项目统一视觉风格和提示词后缀。按 `style/{项目名}/style-guide/style-guide.json` 存放设定文件；项目级风格参考图、视频分别放入该目录下的 `images/`、`videos/`。
- `scripts/epNN/`：每集分镜剧本，`script.json` 为权威数据源，`script.md` 可作为可读版。
- `continuity/`：分集状态、伏笔、角色状态、诊断报告。
- `output/`：最终项目 manifest 和每集提示词包。
- `volcengine-private-assets/`：火山私域素材资产库入库记录；`originals/` 存放实际提交入库的原图副本，`records/` 存放对应 `asset_id`、素材来源、用途和复用说明。

## 漫剧创建流程

1. Phase 1 策划：产出 `planning/story-outline.md` 和 `planning/episodes-plan.md`。
2. Phase 2 设计：产出角色、场景、道具和项目级 `style-guide.json`；设定 JSON 必须放入对应同名文件夹，不直接散落在 `characters/{项目名}/`、`scenes/{项目名}/`、`props/{项目名}/` 根层；角色卡必须包含可区分脸部特征的 `face_profile`。
3. Phase 3 剧本：按集产出 `scripts/epNN/script.json` 和 `continuity/epNN-state.json`。
4. Phase 4 诊断：小说改编项目必须产出 `continuity/diagnosis-epNN.md`，原创项目可选。
5. Phase 5 组装：产出 `output/project-manifest.json` 和 `output/epNN-prompt-package.json`。

## 关键约束

- 本项目所有实际生成的参考图片、分镜图片和视觉素材，统一使用 `gpt-image-2` 生成；不要混用其他图片生成模型，除非用户明确指定。
- 九宫格分镜图片的整张画布比例必须与最终目标视频比例一致，例如目标视频为 `9:16` 时，九宫格分镜图也必须生成 `9:16` 竖版画布；九格是在同一画布内承载连续关键帧，不默认使用 `1:1` 方图。
- 分镜图片提示词中必须使用角色名和角色状态，例如 `陈易（觉醒前）`，不要只写外貌标签。
- 创建或修订角色卡时，必须为每个角色写入 `face_profile`，至少覆盖脸型、肤色、眼睛、眉毛、鼻唇、辨识点和防混淆说明；每个角色变体的 `appearance` 和 `prompt` 也必须同步写入具体面部锚点，避免不同角色生成成同一张脸。群像角色必须明确多人不同脸型和五官差异；非人类角色必须明确“无真实人脸”或对应的非人类视觉形态。
- 生成角色参考图时必须遵守 `characters/角色参考图提示词规范.md`；普通单人角色先生成该角色/变体的面部头像图，再基于头像参考图生成 9:16 正面全身图；不要直接从文本提示词跳到全身图，以保证角色脸部一致性。
- 生成同一角色其他形态的参考图片时，按 `characters/角色参考图提示词规范.md` 执行：先用该角色最先生成的面部头像作为身份参考，生成该形态的面部头像；再用该形态的面部头像作为参考，生成该形态的 9:16 正面全身图。
- 生成场景参考图时默认使用横屏构图，并采用远景镜头呈现场景全貌；不要用近景或局部特写作为场景主参考图。
- 生成道具参考图时默认使用 1:1 正方形构图，主体居中完整呈现，便于后续作为分镜或视频生成的稳定道具参考。
- `scripts/` 下分镜剧本必须遵守 `scripts/script-writing-rules.md`；生成或修订 `script.json`、`script.md` 前先参考该文档。
- 生成或修订分镜剧本时，每个镜头的视频时长默认固定为 `15s`；如确需其他时长，必须先说明原因并得到用户确认。
- `scripts/epNN/script.json` 中每个镜头除基础字段外，还应尽量包含 `detailed_beats`、`visual_focus`、`performance_notes`：`detailed_beats` 按 `0-5s`、`5-10s`、`10-15s` 三段描述视频推进，保证单镜头 15 秒内有明确动作、情绪和画面变化。
- 小说改编项目生成剧本时必须参考原文细节，不要只写剧情摘要；动作、表情、站位、证据呈现、情绪转折和关键对白都要落到镜头描述或 `detailed_beats` 中。
- 视频提示词必须基于分镜图片提示词派生，不要独立另写一套场景和动作。
- 如需把人物或含人物形象的参考图提交到火山私域素材资产库，必须同时把提交入库的原图副本归档到 `volcengine-private-assets/originals/`，并在 `volcengine-private-assets/records/` 写入同名 JSON 记录，至少包含 `asset_id`、`asset_uri`、`group_id`、原始文件路径、入库原图归档路径、用途、状态和后续复用说明。
- 每个镜头必须保持角色、场景、道具引用可追溯到 Phase 2 设计文件；引用对象的权威 JSON 位于 `{类别}/{项目名}/{对象名}/{对象名}.json`。
- `scripts/epNN/script.json` 是剧本权威来源，Markdown 表格只是辅助阅读。
- `output/` 中的 JSON 必须可被标准 JSON 解析器解析，不能包含注释、尾逗号或 Markdown。
- 新增分集目录统一使用两位数编号：`ep01`、`ep02`、`ep03`。

## 协作要求

- 修改已有产物前先确认上下游影响，尤其是角色名、场景名、变体名。
- 不要随意改动目录命名；角色名、场景名、道具名如果改名，必须同步调整同名文件夹、同名 JSON 文件和所有分镜引用。
- 允许把实际生成的轻量参考图片、视频、音频放入对应角色、场景、道具或分集目录的素材子目录中；角色、场景、道具、风格素材优先放入各自设定文件夹下的 `images/`、`videos/`；大体积素材另行归档。
- 完成文件创建或修改后，至少用 `find`、`ls` 或 JSON 校验命令确认结果。
